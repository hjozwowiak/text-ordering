import { red, yellow } from "@material-ui/core/colors";

export const defaultTheme = {
  darkMode: false,
  palette: {
    type: "light",
    primary: red,
    secondary: yellow,
    error: red,
  },
};
